# =============================================================
# deploy/docker-compose.yml
# Deployment manifest for Cats vs Dogs Inference API
#
# Usage:
#   docker compose -f deploy/docker-compose.yml up -d
#   docker compose -f deploy/docker-compose.yml down
#   docker compose -f deploy/docker-compose.yml ps
# =============================================================

version: "3.9"

services:
  cats-dogs-api:
    # Pull latest image from Docker Hub
    image: ${DOCKERHUB_USERNAME:-donald01johnson}/cats-dogs-api:latest

    container_name: cats-dogs-api

    # Always restart unless manually stopped
    restart: unless-stopped

    # Map host port 8000 to container port 8000
    ports:
      - "8000:8000"

    # Environment variables passed into container
    environment:
      - MODEL_PATH=/app/artifacts/model.pt
      - LOG_LEVEL=INFO
      - PYTHONUNBUFFERED=1

    # Mount logs directory from host into container
    # Allows persistent log access even after container restarts
    volumes:
      - ../logs:/app/logs

    # Docker built-in health check
    # Polls /health every 30s; marks container unhealthy after 3 failures
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

    # Resource limits (prevents container from consuming all CPU/RAM)
    deploy:
      resources:
        limits:
          memory: 2g
        reservations:
          memory: 512m

    # Logging configuration
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"
