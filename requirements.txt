# =============================================================
# cats-dogs-mlops — Production Dependencies (version pinned)
# Platform : Ubuntu 22.04 / python:3.10-slim (Docker)
# =============================================================

# PyTorch CPU — installed separately in Dockerfile via extra-index-url
# torch==2.13.0+cpu
# torchvision==0.28.0+cpu

# API Framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Image processing
pillow==10.3.0

# Numerical
numpy==1.26.4

# Monitoring
prometheus-fastapi-instrumentator==7.0.0
prometheus-client==0.26.0
